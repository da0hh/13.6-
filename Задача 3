#include <iostream>
#include <string>
#include <cassert>
#include <vector>

using namespace std;


int main() {
    setlocale(LC_ALL, "RU");
    vector<int> data(20);
    int num, curpos = 0;
    cin >> num;
    while (num != -2) {
        if (num == -1) {
            for (int i = curpos; i < data.size(); i++) {
                cout << data[i] << " \n";
            }
            for (int i = 0; i < curpos; i++) {
                cout << data[i] << " \n";
            }
        }
        else {
            data[curpos] = num;
            curpos++;
            if (curpos == 20) curpos = 0;
        }
        cin >> num;
    }
}
